<%= if logged_in?(@conn) do %>
  <%= link "Sign out", to: auth_path(@conn, :delete, :access), method: :delete, class: "btn btn-danger" %><br>
  <%= current_user(@conn).email %>
<% else %>
  <%= link "Sign in", to: homepage_path(@conn, :index), class: "btn btn-danger" %>
<% end %>
